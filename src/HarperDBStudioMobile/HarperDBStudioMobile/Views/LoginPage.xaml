<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="HarperDBStudioMobile.Views.LoginPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource AppBackgroundColor}">
    <Frame x:Name="loginFrame"
           HorizontalOptions="FillAndExpand"
           Margin="20"
           Padding="20,40,20,40"
           VerticalOptions="CenterAndExpand"
           BackgroundColor="{StaticResource FrameBackgroundColor}">
            <StackLayout>
                <Entry x:Name="user_email" Placeholder="Email"/>
                <Entry x:Name="user_password"  IsPassword="True" Placeholder="Password" />
                <StackLayout Orientation="Horizontal">
                    <CheckBox x:Name="remember_me_checkbox" IsChecked="False" />
                    <Label Text="Remember Me" />
                </StackLayout>
            <Button VerticalOptions="Center" Text="Login" Clicked="Button_Clicked" Margin="0,20,0,0" BackgroundColor="{StaticResource AppBackgroundColor}" BorderColor="#ffffff"/>
            </StackLayout>
    </Frame>
</ContentPage>